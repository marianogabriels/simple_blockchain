#!/usr/bin/env ruby

system("docker build . -t simple_blockchain")
system("docker network create simple_blockchain")

4.times.each do |n|
  system("docker run -it --name simple_blockchain_#{n} --network simple_blockchain simple_blockchain")
end
